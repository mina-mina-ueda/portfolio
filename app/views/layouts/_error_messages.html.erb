<% if resource.errors.any? %>
  <div id="error_explanation">
    <h>エラーが発生しました。<br>
    以下ご確認お願いいたします。</h6>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>