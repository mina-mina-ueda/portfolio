<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-8 mr-5">
      <h3>お客様投稿一覧</h3>
      <table class="table table-hover table-inverse">
      <% @post.each do |post| %>
        <thead>
          <tr>
            <th><%= post.category %></th>
          </tr>
          <tr>
            <th><%= post.title %><%= link_to "詳しく見る", admin_post_path(post), class: "btn btn-secondary" %></th>
          </tr>
        </thead>
      <% end %>
      </table>
    </div>
    <div class="col-md-3">
    	<h3>イベント情報</h3>
      <h5><%#= event_post.title %></h5>
      <h5><%#= event_post.body %></td>
      <%#= link_to "編集する", edit_admin_event_post_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
