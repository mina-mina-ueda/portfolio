  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th><%= @post.category %></th>
      </tr>
      <tr>
        <th><%= @post.title %></th>
      </tr>
    </thead>
    <tbody>
      <td><%= @post.body %></td>
    </tbody>
  <%= form_with model:@response, local: true do |f| %>
  <%= f.submit "返答する", class:"btn btn-lg btn-block btn-info mt-5" %>
  <% end %>
